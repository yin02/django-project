from django.apps import AppConfig

# Configuration for the food app.

class FoodConfig(AppConfig):
    """
    The FoodConfig class extends AppConfig and defines the name of the food app.
    """
    name = 'food'
